require 'spec_helper'

describe RakeMKV do
  describe '.config' do
    it 'creates a single instance of a config' do
      expect(RakeMKV.config).to equal RakeMKV.config
    end
  end
  describe '.confiure'
end
